import { Point } from "./type"

/**
 * 判断点是否在线上
 * @param point
 * @param lineStart
 * @param lineEnd
 */
export function pointInLine(point: Point, lineStart: Point, lineEnd: Point): Boolean {
    return false
}